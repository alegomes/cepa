agent: domain-dev
lane: domain + application (pure business logic, ports, services)

feedback:
  - run_id: 2026-05-25-wego-1682
    date: 2026-05-26
    topic: per-item batch validation fail-fast vs accumulate
    original_choice: Option B — fail-fast per item (mime invalid → skip size + tipo for that item)
    user_verdict: keep
    user_reason: "keep, but I don't have a strong opinion for it"
    tag: example

  - run_id: 2026-05-25-wego-1682
    date: 2026-05-26
    topic: bridge methods that defer breakage to runtime
    original_choice: Option B — add saveAll as default throwing UnsupportedOperationException so existing adapter compiles unchanged
    user_verdict: overrule
    user_reason: "não vamos esconder a sujeira debaixo do tapete"
    refined_rationale: "Prefer compile-fail (abstract method) over runtime-throw bridge. If a downstream Task hasn't implemented something yet, let the compiler force the issue at authoring time, not at runtime in QA/prod. No hidden runtime traps just to keep an intermediate commit green."
    tag: principle

  - run_id: 2026-05-25-wego-1682
    date: 2026-05-26
    topic: nested vs separate file for single-use records
    original_choice: Option B — BatchItemError as nested record inside BatchValidationException
    user_verdict: keep
    user_reason: "(no reason given)"
    tag: example

  - run_id: 2026-05-27-wego-1683
    date: 2026-05-27
    topic: WEGO-1683 tactical batch — T1+T2+T3+T5 inline decisions (mids/lows)
    altitude: tactical
    original_choice: |
      Batch-keep on 8 tactical decisions from the run:
      - Round 2 T1 (AtendimentoNormalizer): skip medico.cpf re-normalization (Medico constructor already strips non-digits); email handled separately via normalizeEmail (RFC 5321 case-insensitive); blank-after-normalize via normalizeStringOrOriginal (validation belongs upstream, not in normalizer).
      - Round 3 T2 (exceptions): extend DomainException not raw RuntimeException (existing mappers filter on DomainException); JavaBean getters on exception + record-style on nested Divergencia (matches BatchValidationException precedent); HisDataDriftException refuses null/empty divergencias list at construction (fail-loud on diff-logic bugs).
      - Round 4 T3 caller routing: BuscarAtendimentosFiltrados / BuscarHistoricoAtendimento / SolicitarAssinatura → importarDoHis (fresh); UploadDocumentoAtendimento + UploadDocumentosBatch → importarOuObterLocal (cached).
      - Round 5 T5: catch RuntimeException in refreshFromHis → convert to HisUnavailableException (operator sees consistent 503 for any gateway hiccup, real cause still logged).
    user_verdict: keep
    user_reason: "batch-keep — debrief pace: altas one-by-one, mids/lows batch-keep (no individual reservation raised)"
    tag: example
