{
  "name": "common",
  "version": "0.20.0",
  "description": "Shared mindset skills required by the alegomes topology plugins. Ships mental-model, active-listener, zero-micromanagement, conversational-response, till-done, autonomous-mode, green-or-revert, and acceptance-completeness. Agents: completion-auditor (independent last-mile acceptance gate), ui-proof-reviewer (independent proof gate for the UI/extension surface \u2014 pinned Playwright flows from .claude/ui-proof.yaml, fresh backend asserts, green\u2192red\u2192green perturbation). Commands: /doctor (valida a instala\u00e7\u00e3o do harness \u2014 plugins, hooks, baseline, worktrees, handoffs, plus the operational-update checks: an install that died halfway leaves status=in_progress in ~/.claude/ops/last-install.json and is reported as a FAILURE rather than silence, live-vs-repo drift carries the age of the last install, and a recent --rollback is called out because the live cache is then the one BEFORE the install you attempted), /metrics (telemetria do pr\u00f3prio harness: sess\u00f5es, builds, bloqueios de gate, vereditos de prova \u2014 ledger em ~/.claude/cepa-telemetry alimentado pelos hooks via _telemetry.py), /autonomous-start, /autonomous-resume, /debrief, /recap, /handoff (save a rich session handoff for seamless resume), /branch, /return, and the per-session worktree lifecycle (/worktree-start, /worktree-merge, /worktree-list, /worktree-discard, /worktree-name, /worktree-label \u2014 get/set a durable free-text purpose stored as the branch description) for parallel `claude` windows on one repo, plus /wrap-up \u2014 the one-shot session-shutdown chain (commit \u2192 handoff \u2192 merge into the integration branch \u2192 push \u2192 prune, behind a single confirmation; --discard for dead ends) that reuses worktree-merge's green-gate / single-owner / conflict-stop guards. Ships plan-schema.yaml \u2014 the CANONICAL annotated plan.yaml (schema v1), shared by two sibling consumers with no dependency between them: maestro's `mode: parallel-waves` (waves of slices in worktrees) and board-flow's `mode: single-track` (one item at a time across sessions, carrying the execution order, the `why` behind each position, and the `human_pending` left for the human after a card closes). `mode` is optional and defaults to parallel-waves, so every plan.yaml written before the field existed stays valid and every existing consumer keeps working \u2014 the reason schema_version was NOT bumped. Bin: cepa-dor \u2014 invocable Definition-of-Ready check for plan.yaml slices (schema v1), which now refuses a single-track plan by pointing at /board-flow:triage and /board-flow:next instead of failing downstream with \"no pending wave\": disjoint surfaces expanded at the fork point, enforcement-zone veto, resolved human gates, executable acceptance (mandatory in no-build repos), green baseline, >=4-demand floor, and the BDD-or-substrate form check (an acceptance that only names private implementation steps is NOT-READY unless the slice declares acceptance_form: substrate); first client is maestro's intake gate, board-flow triage/drain are foreseen consumers. Launcher: bin/ccw \u2014 drop-in for `claude` that auto-isolates a session into its own git worktree only when another live session already occupies the tree; `ccw -s <name>` forces/resumes a named worktree, and `ccw -s` with no name opens an interactive picker (fzf, else a numbered menu) of recent session/* worktrees \u2014 terminal-agnostic, unlike Tab completion which Warp's own engine ignores. Completions: completions/_ccw \u2014 zsh tab-completion for `ccw -s <slice>`, offering existing session/* worktrees newest-first, each captioned with its label (works in standard terminals; Warp ignores zsh completions, so use the `ccw -s` picker there). Hooks: autonomous-checkpoint (PostToolUse on Task), session-log (UserPromptSubmit), session-subject (UserPromptSubmit \u2014 lexical subject-shift detection + wrap-up/handoff nudge; points at /common:wrap-up when the session is landable, else /common:handoff), session-activity (PostToolUse Edit|Write|MultiEdit \u2014 records touched dirs), session-registry (SessionStart/SessionEnd \u2014 live-session registry, overlap warning, worktree auto-clean + unmerged reminder, WIP-autosave, transparent handoff resume, deferred worktree prune-on-exit so /wrap-up never deletes its own live cwd), session-checkpoint (Stop \u2014 continuous mechanical handoff skeleton at .claude/handoffs/<branch>.md, crash-proof), mark-build-stale + capture-build-result + gate-advance (the green-or-revert state machine; worktree-aware \u2014 a build or edit in an out-of-root proof worktree records/skips THERE, never poisoning the main baseline with a deliberate perturbation RED), enforcement-guard (PreToolUse Bash + Edit/Write/MultiEdit \u2014 blocks subagents from writing the plugin/hook/settings enforcement surface), maven-reactor-guard (PreToolUse Bash \u2014 blocks `mvn`/`mvnw` with `-pl`/`--projects` but no `-am` when the cwd is a multi-module reactor: without `-am` Maven resolves sibling modules from the possibly-stale `~/.m2` instead of recompiling, producing a deterministic-but-FALSE red that reads as a functional bug; `-amd` does not count, single-module repos are never gated, a quoted `mvn` inside echo/grep is not an invocation, and `stale-ok` in the command is the explicit escape hatch for a freshly-installed reactor \u2014 WEGO-1949, where documenting it twice failed to stop the recurrence), ui-proof-verdict-guard (PreToolUse Write \u2014 blocks a .claude/proof/ui-*.yaml declaring verdict: proven when any flow is not pass or a perturbation survived/assumed), lead-no-worktree (PreToolUse on Task \u2014 blocks worktree-isolated leads), acceptance-gate (PreToolUse on transitionJiraIssue \u2014 blocks In-Review while the acceptance audit is incomplete), summary-nulls-gate (PreToolUse on addCommentToJiraIssue/jira_add_comment \u2014 blocks an Implementation Summary comment missing the explicit-null fields: New debt introduced, Scope captured outside the card, Release needed, Human validation route; when the declared debt is anything other than none/unknown it additionally requires a Revisit trigger, so debt that outlives the card carries the condition that brings it back), handoff-seeds-gate (PreToolUse Edit/Write/MultiEdit on docs/discovery/*/handoff.md \u2014 blocks a delivery brief missing labeled Validation seeds / Carry-forward notes; an explicit `none` is a valid answer, silence is not. Seeds thought up AFTER the build describe what got built, and discovery's implementation-relevant findings are otherwise re-discovered the expensive way), bounce-reason-gate (PreToolUse on the same comment tools \u2014 blocks a bounce-back comment, i.e. one whose heading announces UNPROVEN / returning for rework / devolu\u00e7\u00e3o, that carries no labeled Reason:/Motivo: with text; the next session must not have to re-derive WHY from the diff). Helpers: _wtlib.py + session-launch.py + session-worktrees.py (worktree engine), _handoff.py (handoff file format + zone-preserving IO), expertise-append.py (flock-serialized append+prune for the shared mental-model.yaml files).",
  "author": {
    "name": "alegomes"
  },
  "keywords": [
    "agents",
    "skills",
    "shared",
    "autonomous",
    "green-or-revert"
  ],
  "skills": "./skills/",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/gate-advance.py\""
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/enforcement-guard.py\""
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/maven-reactor-guard.py\""
          }
        ]
      },
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/enforcement-guard.py\""
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/handoff-seeds-gate.py\""
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/ui-proof-verdict-guard.py\""
          }
        ]
      },
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/lead-no-worktree.py\""
          }
        ]
      },
      {
        "matcher": "mcp__.*transitionJiraIssue",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/acceptance-gate.py\""
          }
        ]
      },
      {
        "matcher": "mcp__.*(addCommentToJiraIssue|jira_add_comment)",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/summary-nulls-gate.py\""
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/bounce-reason-gate.py\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/autonomous-checkpoint.py\""
          }
        ]
      },
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/mark-build-stale.py\""
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/editorial-lint.py\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/capture-build-result.py\""
          }
        ]
      },
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session-activity.py\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session-log.py\""
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session-subject.py\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session-checkpoint.py\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session-registry.py\""
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session-registry.py\""
          }
        ]
      }
    ]
  }
}
