{
  "name": "maestro",
  "version": "0.1.0",
  "description": "Multi-harness orchestration — plan a universe of demands into waves of concurrent Claude Code sessions and land them with one action. v1 design (rev2.1) at .claude/programs/maestro/design.md: /maestro:program-plan turns BACKLOG demands into .claude/programs/<name>/plan.yaml (schema v1) and gates every slice through the invocable Definition-of-Ready check (common/bin/cepa-dor — READY/NOT-READY with named gaps: disjoint surfaces expanded at the fork point, enforcement-zone veto, resolved human gates, executable acceptance, green baseline, >=4-demand floor). Gatekeeper spike (permission mediation for headless children via --permission-prompt-tool over shared loopback MCP HTTP, shadow-mode capable) proven 2026-07-15 in spike-gatekeeper/. /maestro:run (fork + event loop + merge train + wave-state/resume) is construction step 3 — not shipped yet. Requires common@cepa.",
  "author": {
    "name": "alegomes"
  },
  "keywords": ["orchestration", "multi-session", "waves", "worktrees", "gatekeeper"]
}
