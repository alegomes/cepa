{
  "name": "book",
  "version": "0.1.0",
  "description": "Book-writing topology — 2 leads + 10 workers for authoring technical books. Three commands: /book:inception (audience + chapter map), /book:write-chapter (outline→research→draft→code→exercises→review loop), /book:finalize (continuity review + LaTeX compilation). Path-lock keyed to manuscript/ and code/. Standalone — no external tracker.",
  "author": {
    "name": "alegomes"
  },
  "keywords": ["agents", "book", "writing", "technical-writing", "authoring"],
  "skills": "./skills/",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/path-lock.py\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/bash-path-lock.py\""
          }
        ]
      }
    ]
  }
}
