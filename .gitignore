__pycache__/
*.pyc
.DS_Store

# Research scaffold — indydev Dan source material, YouTube transcripts/frames.
# Useful local context, not part of the plugin.
multi-agent/

# Accidental self-referential symlink occasionally created in
# common/expertise/expertise by manual ln -s typos or wrong-host
# bin/install.sh invocations. Insync syncs it across clones; if it
# reappears, rm in BOTH clones simultaneously (Insync resurrects it
# otherwise). See conversation 2026-05-21 / 2026-05-26.
common/expertise/expertise

# Session intent log (session-log hook scratch)
.claude/session-log.md

# Estado de runtime do harness rodando SOBRE este repo — escrito por hooks e
# comandos, específico desta máquina/sessão. O que é fonte dentro de .claude/
# (no-build, programs/**) segue rastreado de propósito.
.claude/doctor-last-run
.claude/last-build.json
.claude/session-mode
.claude/sessions/
.claude/handoffs/
# Artefatos salvos de um worktree removido (rescue do session-registry). Caixa
# de entrada: você move o que interessa para o lugar certo e apaga o resto.
.claude/rescued/

# Symlink criado pelo bin/install.sh apontando para o clone canônico
# (~/coding/cepa/common/expertise). Scaffolding de instalação, não fonte.
.claude/expertise

# Resultados da suite de tarefas douradas: medicao local, nao artefato do repo
tests/eval/results/
