schema_version: 2
card: cepa-until
verdict: proven
reviewed_at: 2026-08-30T18:35:00-03:00
base_commit: b6dbb23
head_commit: 739721b       # 3ª rodada: fecha o nível l4 (entrada adversária) que
                             # ficou `skipped` na rodada anterior (head 75129bc,
                             # needs-human sem achado). Escopo desta rodada = o
                             # diff 75129bc..739721b: tests/test_cepa_until_adversarial.py
                             # (novo), o `except OSError` em common/bin/cepa-until,
                             # e a correção de shebang em fake_claude
                             # (tests/test_cepa_until.py). As 5 classes de
                             # comportamento já provadas em rodadas anteriores
                             # (kill de grupo, --until, warnings, blocked,
                             # guarda de branch) não mudaram entre 75129bc e
                             # 739721b (`git diff --stat` confirma) e não foram
                             # reperturbadas — herdam o PASS já registrado.
issue_type: Feature
repo_kind: harness             # NOT Quarkus/Maven — Python CLI + prompt files, própria suíte em tests/
scope:
  changed_classes:
    - class: "common/bin/cepa-until (executa_item — Popen sem tratamento estourava quando o `claude` some/corrompe DEPOIS da largada; agora `except OSError` devolve -2, sem progresso, disjuntor encerra)"
      module: cli
      external_observable: true
      substituted_by_double: null
    - class: "common/bin/cepa-until (main — mapeamento do código -2 para a razão de relatório 'não consegui nem lançar o `claude`', distinta de 'claude saiu <código>')"
      module: cli
      external_observable: true
      substituted_by_double: null
    - class: "tests/test_cepa_until_adversarial.py (novo — nível l4: linha de comando + estado em disco mutável)"
      module: test-harness
      external_observable: false
      substituted_by_double: null
    - class: "tests/test_cepa_until.py (fake_claude — shebang trocado de `env python3` para sys.executable, para não depender do python3 do sistema ter PyYAML)"
      module: test-harness
      external_observable: false
      substituted_by_double: null
  base_commit_resolved: true
levels:
  l3_load_bearing:
    technique: perturbation
    pit:
      status: n/a
      results:
        - "não há pitest neste repo (não é JVM); perturbação manual no binário real, via subprocess, é o único mecanismo aplicável"
    perturbation:
      status: pass
      results:
        - "PASS (RED) — defeito real desta rodada fechado: removido o `try/except OSError` ao redor do `subprocess.Popen` em executa_item — sem ele, `test_claude_corrompido_no_meio_e_falha_contada_nao_traceback` não falha graciosamente: o `PermissionError` sobe sem tratamento, o supervisor estoura no meio do item, e o teste TAMBÉM quebra (IndexError ao procurar um `run_end` que nunca foi escrito, porque o processo morreu antes de fechar o registro). Restaurado."
        - "PASS (RED) — a mensagem específica de relatório também é load-bearing, não só o código de saída: removido o ramo `'não consegui nem lançar o `claude`' if codigo == -2` (cai no genérico 'claude saiu -2') — a asserção '...e o relatório nomeia que nem deu para lançar' (checa a palavra 'lançar' no stdout) foi para FAIL sozinha, as outras 3 do mesmo teste continuaram verdes. Confirma que o teste distingue a causa raiz (falha ao lançar) de um exit code genérico, não só verifica 'terminou mal'. Restaurado."
        - "REVISADO ESTRUTURALMENTE, NÃO EXECUTADO (por instrução explícita desta reprova — 'NÃO rode nada que possa invocar o claude real'): o achado (a) do próprio implementador (`PATH` herdado deixava o `execvp` cair no `claude` DE VERDADE quando o falso virava diretório) foi conferido sem repetir o acidente. `roda()` monta `PATH = f'{binv}:{PATH herdado}'`; `echo $PATH` neste ambiente mostra `/Users/alegomes/.local/bin` — onde `which claude` aponta o binário real — na 1ª posição do PATH herdado. Editei `test_claude_corrompido_no_meio_e_falha_contada_nao_traceback` para remover o `\"PATH\": \"/usr/bin:/bin\"` do `extra_env` (deixando o PATH herdado passar), mas NÃO executei esse teste perturbado — rodar significaria repetir os 87s de agente real que o implementador relatou. A leitura estática do `roda()` e do `PATH` do ambiente é suficiente para confirmar que, sem a correção, o teste chamaria o binário real; não contabilizo isso como perturbação verde→vermelho completa, é evidência estrutural complementar."
        - "grep em tests/*.py confirma que nenhum OUTRO teste da suíte substitui um binário por um diretório nem depende de fallback de PATH — os dois usos remanescentes de PATH herdado (test_install_ops.py:118,157) só prependem um stub válido, nunca simulam corrupção; não há mais nenhum caso da mesma família."
        - "Não reperturbado nesta rodada (arquivos não mudaram entre 75129bc e 739721b, `git diff --stat` confirma escopo restrito a common/bin/cepa-until, tests/test_cepa_until.py, tests/test_cepa_until_adversarial.py, .claude/acceptance/cepa-until.yaml): as 5 perturbações da rodada anterior (kill de grupo/timeout, --until, propagação de warnings, contagem blocked, guarda de branch) permanecem PASS herdado."
      run: >-
        worktree descartável /tmp/proof-cepa-until-3 (`git worktree add
        --detach 739721b`, worktree principal nunca tocado; edições de
        perturbação usaram caminho ABSOLUTO por causa do path-lock deste
        agente, que bloqueia caminho relativo mesmo fora da raiz do repo
        principal — não editei o hook). GREEN de partida confirmado nos dois
        arquivos primeiro: `timeout 100 python3 tests/test_cepa_until.py` →
        "✓ tudo verde"; `timeout 90 python3
        tests/test_cepa_until_adversarial.py` → "✓ tudo verde" (45 checks, 0
        FAIL). Cada perturbação: edição via script Python com `assert old in
        s` antes de escrever, depois `timeout 90 python3
        tests/test_cepa_until_adversarial.py` exigindo FAIL, depois `git -C
        /tmp/proof-cepa-until-3 checkout -- <caminho absoluto>` restaurando e
        confirmando GREEN de novo antes da perturbação seguinte.
  l2_coverage:
    status: pass
    uncovered_lines: []
    run: >-
      O `except OSError` novo e o ramo de mensagem `codigo == -2` ficaram
      VERMELHOS ao serem quebrados (ver l3 acima) — não estão descobertos.
      Leitura estrutural do diff 75129bc..739721b em common/bin/cepa-until
      (22 linhas) não achou hunk sem teste correspondente.
  l4_adversarial_input:
    status: clean
    reason: ""
    findings:
      - "kill ratio informal: das 7 entradas de `--for` testadas como absurdas (0h, 0m, -3h, 1e9h, h, 12hh, vazio), todas as 7 foram recusadas com rc=2 e zero disparo — nenhuma escapou para a aritmética ou para o subprocess."
      - "janela gigante (99999h, 1000000m) não estourou `OverflowError`: ou vira uma data válida ou é recusada, sempre com veredito (rc 0 ou 2), nunca traceback."
      - "metacaractere de shell no nome da fila (`fila; touch <canário>`) não executou nada — confirma que o comando é lista, nunca `shell=True`."
      - "plan.yaml corrompido/apagado NO MEIO do run (não na largada) encerra por `fila-ilegivel` com o registro fechado, não trava nem estoura."
      - "o `claude` virando diretório DEPOIS da largada — a superfície mais perigosa, porque quebra sem qualquer aviso prévio — agora é contida: item sem progresso, disjuntor encerra em 3, relatório nomeia a causa raiz."
      - "achado colateral aceito, não é gap do produto: os 2 defeitos que o PRÓPRIO implementador descreveu nos testes (PATH herdado achando o claude real; shebang sem PyYAML) eram do arnês de teste, não do cepa-until — confirmados via leitura estrutural acima, não reintroduzidos."
    run: >-
      `timeout 90 python3 tests/test_cepa_until_adversarial.py` → "✓ tudo
      verde" (45 checks, 0 FAIL) na baseline 739721b, worktree descartável.
      As duas superfícies de entrada da instrução desta reprova (linha de
      comando E estado em disco mutável) estão cobertas pelo arquivo; ver
      l3_load_bearing.perturbation para as 2 perturbações verde→vermelho que
      confirmam que o mecanismo de defesa (não só a suite) é load-bearing.
  bugfix_regression_red_at_base:
    status: n/a
    run: "issue_type Feature, não Bug — não há regressão a reproduzir na base"
routing_reason: >-
  O nível que travava a rodada anterior em NEEDS-HUMAN (l4_adversarial_input:
  skipped) está fechado: existe agora um arquivo de teste dedicado
  (tests/test_cepa_until_adversarial.py) cobrindo as duas superfícies de
  entrada — linha de comando e estado em disco mutável durante o run — e ele
  roda GREEN na baseline. O defeito real que motivou esta rodada (Popen sem
  tratamento estourando quando o `claude` some/corrompe depois da largada)
  foi perturbado (removido o `except OSError`) e foi para VERMELHO, junto com
  a mensagem de relatório que nomeia a causa raiz. O achado (a) do próprio
  implementador (isolamento de teste vazando pelo PATH herdado) foi conferido
  por leitura estrutural, sem repetir a invocação do agente real — não
  encontrei nenhum outro teste da suíte com a mesma dependência de PATH
  herdado. Não achei nenhum comportamento externally-observable sobrevivendo
  quebrado nesta rodada, e o único gap estrutural que impedia `proven` na
  rodada anterior foi fechado com evidência real (não assumida). Por isso
  PROVEN — a única ressalva que continua fora de escopo, por decisão
  explícita desde o início, é que uma janela real de 12h com o agente de
  verdade nunca foi exercida; isso é risco operacional monitorado no primeiro
  run de produção, não um gap de prova.
