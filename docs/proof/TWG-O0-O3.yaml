schema_version: 2
card: TWG-O0-O3
verdict: proven
reviewed_at: 2026-09-24T16:52:00-03:00
base_commit: 25f1c17
head_commit: f3e6ddc
issue_type: N/A            # sem card Jira; sequência O0+O3 de docs/archive/estrategia-twg-vs-mcp.md
repo_kind: harness         # NOT Quarkus/Maven — Python + prompt files, suíte própria em tests/*.py
scope:
  changed_classes:
    - class: "common/hooks/jira-write-lock.py (barra escrita twg de agente não autorizado; libera atlassian-expert; libera sessão principal; libera leitura)"
      module: hook
      external_observable: false
      substituted_by_double: null
    - class: "common/hooks/_jiramut.py (`--help`/-h não é escrita; leituras de terceiro nível _TWG_JIRA_READ_SUB; create/link/update via _legivel; _UPDATE_OPACO; TWG_VERIFICADA)"
      module: hook-lib
      external_observable: false
      substituted_by_double: null
    - class: "board-flow/agents/atlassian-expert.md (linha `tools:` sem os 32 nomes dos dois servidores MCP locais, com Bash; seção de binding com o detector `command -v twg && twg --version`)"
      module: agent-spec
      external_observable: false
      substituted_by_double: null
    - class: "common/.claude-plugin/plugin.json (registro do hook jira-write-lock.py no array PreToolUse)"
      module: plugin-manifest
      external_observable: false
      substituted_by_double: null
  base_commit_resolved: true
  note: >-
    Diff 25f1c17..cfa3054 (commits 937fc1b, d70e366, cfa3054), interseccionado
    com os arquivos indicados pelo usuário. Rodada 2 (este arquivo): o
    coordenador fechou a lacuna da rodada 1 no commit f3e6ddc
    ("test(common): registro das travas e dos gates de Jira no plugin.json"),
    que muda só tests/test_jiramut_deteccao_por_efeito.py — nenhum arquivo de
    produção mudou entre cfa3054 e f3e6ddc (confirmado por
    `git diff --stat cfa3054..f3e6ddc`: só o arquivo de teste). As nove
    perturbações da rodada 1 continuam válidas sem precisar ser refeitas — a
    linha que cada uma quebra não mudou. Refeita apenas a décima
    (plugin.json), contra o novo bloco de teste.
levels:
  l3_load_bearing:
    technique: perturbation
    pit:
      status: n/a
      results:
        - "não há pitest neste repo (não é JVM); perturbação manual dos hooks Python, via subprocess.run e import direto, é o mecanismo aplicável"
    perturbation:
      status: pass
      results:
        - "REAPROVEITADO da rodada 1 (nenhuma linha de produção mudou entre cfa3054 e f3e6ddc) — PASS (RED): jira-write-lock.py:53-55 (condição de liberação trocada por `sys.exit(0)` incondicional) derrubou o bloco '--- jira-write-lock ---' de 11/11 para 6/11 (5 falhas: dev cria card, dev transiciona, dev comenta, embutido sem prefixo edita, dev por twg api POST)."
        - "REAPROVEITADO — PASS (RED): jira-write-lock.py:36 (`AUTHORIZED` trocado por valor que nunca bate) derrubou o mesmo bloco para 9/11 (atlassian-expert cria/transiciona passaram a exit=2)."
        - "REAPROVEITADO — PASS (RED): jira-write-lock.py:54 (`not agent_type` removido) derrubou o mesmo bloco para 10/11 ('sessao principal (humano)' passou a exit=2)."
        - "REAPROVEITADO — PASS (RED): _jiramut.py:282-283 (checagem de `--help`/`-h` removida) — 2 falhas ('--help nao e escrita', '-h nao e escrita')."
        - "REAPROVEITADO — PASS (RED): _jiramut.py:102 (`(\"workitem\",\"link\",\"query\")` removido de _TWG_JIRA_READ_SUB) — 1 falha ('link query e leitura')."
        - "REAPROVEITADO — PASS (RED): _jiramut.py:303-304 (branch `create` → `_legivel` removido) — 2 falhas ('escondido apos ; ainda e achado', 'create legivel')."
        - "REAPROVEITADO — PASS (RED): _jiramut.py:110-113 (`--status` removido de _UPDATE_OPACO) — 1 falha ('update --status transiciona por fora')."
        - "REAPROVEITADO — PASS (RED): _jiramut.py:97 (TWG_VERIFICADA trocado para 1.9.9) — tests/test_atlassian_expert_spec.py, 1 falha."
        - "REAPROVEITADO — PASS (RED): atlassian-expert.md (Bash removido de `tools:`) — tests/test_atlassian_expert_spec.py, 1 falha ('Bash ligado para a twg')."
        - "REAPROVEITADO — PASS (RED): atlassian-expert.md (detector `command -v twg && twg --version` trocado) — tests/test_atlassian_expert_spec.py, 1 falha ('detector de caminho é um comando só')."
        - "REFEITA NESTA RODADA — PASS (RED): common/.claude-plugin/plugin.json (bloco de registro do hook `jira-write-lock.py` no array PreToolUse/Bash removido, JSON mantido válido): tests/test_jiramut_deteccao_por_efeito.py caiu (exit 1), bloco novo '--- registro no plugin.json (PreToolUse, matcher Bash) ---' com 'FALHOU: jira-write-lock.py nao esta registrado no matcher Bash'. Restaurado com `git checkout HEAD -- common/.claude-plugin/plugin.json`; suíte voltou a exit 0 com as 6 entradas (jira-write-lock, bitbucket-decision-lock, acceptance-gate, merge-truth-gate, summary-nulls-gate, bounce-reason-gate) todas 'ok'."
      run: >-
        Rodada 2. Worktree descartável /tmp/proof-TWG-O0-O3-2 (`git worktree
        add --detach f3e6ddc`). Worktree principal
        /Users/alegomes/cepa-worktrees/cepa-pendencias_ai_memory NUNCA tocado
        (`git status --short` nele mostrou só o próprio docs/proof/TWG-O0-O3.yaml
        untracked desta sessão, antes e depois). GREEN de partida:
        `python3 tests/test_jiramut_deteccao_por_efeito.py` → exit 0, "11/11
        passaram" + as 6 entradas do bloco novo 'ok'. Perturbação via
        `python3 -c`/heredoc em caminho absoluto dentro do worktree
        descartável (removida a entrada de jira-write-lock.py do array
        PreToolUse, JSON validado com `json.load` antes de rodar o teste):
        `python3 tests/test_jiramut_deteccao_por_efeito.py` → exit 1, "FALHOU:
        jira-write-lock.py nao esta registrado no matcher Bash". Restaurado
        com `git checkout HEAD -- common/.claude-plugin/plugin.json`
        (`git diff --stat` voltou vazio); reexecutado:
        `python3 tests/test_jiramut_deteccao_por_efeito.py` → exit 0, as 6
        entradas 'ok' de novo. Todas as edições em primeiro plano, nenhum
        `run_in_background`. Worktree removido com `git worktree remove
        --force /tmp/proof-TWG-O0-O3-2`; `git worktree list` ao final mostra
        só a árvore principal e as outras árvores já existentes na sessão,
        sem sobra.
  l2_coverage:
    status: pass
    uncovered_lines: []
    run: >-
      A linha antes descoberta (registro de jira-write-lock.py no
      plugin.json) agora tem cobertura direta: o bloco novo em
      tests/test_jiramut_deteccao_por_efeito.py carrega o plugin.json de
      verdade e confere as 6 entradas (jira-write-lock, bitbucket-decision-
      lock e os 4 gates) no matcher Bash do PreToolUse — confirmado por
      perturbação acima indo para RED. Nenhuma linha comportamental do diff
      ficou sem teste que a quebre.
  l4_adversarial_input:
    status: n/a
    reason: >-
      Nenhum dos artefatos do diff parseia entrada vinda de fora do processo
      além da linha de comando `twg`, já coberta pelos casos adversariais de
      tests/test_jiramut_deteccao_por_efeito.py (aspas não fechadas, `>=`
      como falso redirect, `twg api` com corpo em arquivo, escapatórias por
      `;`/`&&`). Não há módulo do tipo `api-rest` nesta topologia
      (repo_kind: harness).
  bugfix_regression_red_at_base:
    status: n/a
    run: "não é card de tipo Bug — não há regressão a reproduzir na base"
routing_reason: >-
  Rodada 2. O coordenador fechou a lacuna estrutural da rodada 1
  (tests/test_jiramut_deteccao_por_efeito.py ganhou, no commit f3e6ddc, um
  bloco que carrega common/.claude-plugin/plugin.json de verdade e confere
  as 6 entradas de PreToolUse/Bash). Refiz só a perturbação da décima linha
  em worktree descartável /tmp/proof-TWG-O0-O3-2: removida a entrada de
  jira-write-lock.py, o teste foi para VERMELHO ('FALHOU: jira-write-lock.py
  nao esta registrado no matcher Bash'); restaurada, voltou para VERDE. As
  outras nove perturbações da rodada 1 continuam válidas sem precisar ser
  refeitas, porque nenhuma linha de produção mudou entre cfa3054 e f3e6ddc
  (só o arquivo de teste mudou). As dez linhas de comportamento pedidas
  estão, agora, cada uma guardada por um teste que vai para vermelho quando
  ela quebra. Não há mais lacuna nomeada nem lacuna estrutural: PROVEN.
