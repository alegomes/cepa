schema_version: 2
card: P1-sweep-mode
verdict: proven
reviewed_at: 2026-08-23T00:00:00-03:00
base_commit: 4614c5a
head_commit: 995625c
issue_type: Feature
scope:
  changed_classes:
    - class: "maestro/commands/program-plan.md"
      module: maestro-commands-prosa
      external_observable: true
      substituted_by_double: null
    - class: ".claude/programs/lotes-2026-08-23/plan.yaml"
      module: maestro-programs-artefato-real
      external_observable: true
      substituted_by_double: null
    - class: "tests/test_program_plan_sweep.py"
      module: tests
      external_observable: false
      substituted_by_double: null
levels:
  l3_load_bearing:
    technique: perturbation
    pit:
      status: n/a
      results:
        - "program-plan.md é interaction:conversational (frontmatter) — prosa seguida por um agente Claude numa sessão interativa, não código executado por interpretador. Não há função/bytecode para PIT mutar; a técnica aplicável é perturbação textual da prosa + perturbação do artefato real que a prosa produziu, avaliado por um segundo mecanismo determinístico (cepa-dor)."
    perturbation:
      status: pass
      results:
        - "EIXO 1 (trava de regressão da prosa é real, não cosmética): worktree descartável /tmp/proof-P1-sweep-mode-axis1 (git worktree add em 995625c). GREEN antes (`python3 tests/test_program_plan_sweep.py` -> all green, 8/8 checks). Perturbação A: troquei 'Leia a fonte INTEIRA' por 'Leia um trecho qualquer' -> RED especificamente no check 'varre a fonte inteira (não só --demandas)', 1 failure, exit=1; os outros 7 checks continuaram ok (prova que o teste é específico, não um match genérico). Revertido (`git checkout 995625c -- maestro/commands/program-plan.md`)."
        - "Perturbação B (mesmo worktree): troquei a menção a 'rode `python3 common/bin/cepa-hotspots`' por texto genérico -> RED especificamente no check 'invoca cepa-hotspots para popular cartorios', 1 failure, exit=1; os outros 7 continuaram ok. Revertido, `python3 tests/test_program_plan_sweep.py` -> all green de novo. Worktree removido (`git worktree remove --force`); `git status --porcelain` na árvore principal ficou limpo de qualquer alteração de source (só arquivos untracked pré-existentes de outros cards, não tocados nesta auditoria)."
        - "EIXO 2 (o plan.yaml real não é aceito só por existir — o cepa-dor de fato o avalia): rodei `python3 common/bin/cepa-dor .claude/programs/lotes-2026-08-23/plan.yaml --wave 1 --repo .` sem perturbar nada na árvore principal -> 3/3 slices READY (S1, S2, S3), exit=1 (só avisos ⚠, nenhum NOT-READY) — isto É o CS-1 passando ao vivo, run real registrado abaixo."
        - "Worktree descartável separado /tmp/proof-P1-sweep-mode-axis2 (git worktree add em 995625c). Copiei o plan.yaml real e o perturbei de propósito: adicionei 'board-flow/agents/atlassian-expert.md' (já presente em cartorios: e na surface de S1) também à surface de S2, forçando duas superfícies a intersectarem num arquivo-cartório real — exatamente o veto que P3-cepa-dor-cartorio provou existir. Rodei `cepa-dor --wave 1 --repo .` sobre a cópia perturbada -> S1 e S2 NOT-READY ('superfície intersecta a de S2/S1 num arquivo-cartório'), 1/3 READY, exit=2. RED confirmado no gate mecânico."
        - "Contraste: rodei o cepa-dor de novo sobre o plan.yaml ORIGINAL (não perturbado, na árvore principal) -> voltou a 3/3 READY, exit=1 — o mesmo binário, mesmo comando, mesma lógica, aprovando o original e reprovando a cópia quebrada é a prova de que o gate está lendo o conteúdo real, não decorando um resultado fixo. Worktree removido (`git worktree remove --force`); `git status --porcelain` limpo."
        - >-
          EIXO 3 (2026-08-23, réplica independente e cega — não substitui L2,
          reforça L3): um segundo agente, sem contexto desta sessão e sem
          saber o que estava sendo provado, recebeu só a instrução de seguir
          maestro/commands/program-plan.md do zero em --sweep, numa fatia do
          BACKLOG.md diferente da já usada em
          .claude/programs/lotes-2026-08-23/plan.yaml. Ele produziu de forma
          independente um segundo artefato real,
          <raiz-principal>/.claude/programs/lotes-sweep-verify/plan.yaml
          (schema_version 2, mode parallel-waves, campo cartorios: com 7
          arquivos populado via cepa-hotspots, onda 1 com 3 slices [S1,S2,S3],
          onda 2 com a slice sensível S4 sozinha por tocar
          build-hex/hooks/path-lock.py — zona de enforcement). EU MESMO
          (proof-reviewer), não o agente que gerou o plano, rodei ao vivo
          nesta sessão: `python3 common/bin/cepa-dor
          /Users/alegomes/Insync/alegomes@gmail.com/GoogleDrive/2026/coding/cepa/.claude/programs/lotes-sweep-verify/plan.yaml
          --wave 1 --repo /Users/alegomes/cepa-worktrees/cepa-parallel_batches`
          -> 3/3 slices READY (S1, S2, S3), exit=1 (só avisos: baseline
          dispensada por .claude/no-build, 7 arquivos-cartório fora de toda
          superfície, e um aviso de que nenhum caminho existente casa com a
          superfície de S1). Confirmei também `--wave 2` -> 0/1 READY, exit=2,
          S4 NOT-READY por duas razões batendo com o relatado pelo agente
          fresh: superfície intersecta a zona de enforcement (43 arquivos, ex.
          build-hex/hooks/path-lock.py) e decisão estratégica em aberto
          (human_gate: aberta). Os dois exit codes e as duas listas de motivo
          batem exatamente com o que o agente fresh relatou, sem eu ter
          alimentado nada do resultado esperado antes de rodar. Isto é uma
          SEGUNDA amostra independente de que a prosa é seguível
          corretamente por um agente que não sabe que está sendo avaliado —
          fortalece a confiança no comportamento típico, mas continua sendo
          amostragem comportamental (mais um ponto de dados), não medição de
          cobertura formal (essa lacuna foi fechada em separado, ver
          l2_coverage abaixo).
  l2_coverage:
    status: pass
    uncovered_lines: []
    run: >
      A lacuna de instrumentação registrada no veredito anterior (não existir
      ferramenta que medisse "quanto da prosa foi de fato exercitada" pelo
      teste de regressão) foi fechada por uma ferramenta nova,
      common/bin/cepa-promptcov, cujo próprio veredito
      (docs/proof/promptcov-tool.yaml) é PROVEN: outro proof-reviewer
      perturbou as 3 decisões centrais do script (filtro --tag, detecção de
      checkpoint órfão, recusa por vacuidade quando não há checkpoint algum)
      em worktree descartável e confirmou RED nos casos exatos esperados, com
      GREEN restaurado a cada reversão — conferi o artefato e ele contém as
      linhas de run reais, não é auto-certificação. Reproduzi eu mesmo, agora,
      na árvore principal (não em worktree, pois é leitura pura, sem
      perturbação): `python3 common/bin/cepa-promptcov
      maestro/commands/program-plan.md tests/test_program_plan_sweep.py` ->
      saída lista os 4 blocos `[sweep:full-source]`, `[sweep:solo-wave]`,
      `[sweep:wave-cap]`, `[sweep:cartorios]` cada um com "✓", "4/4
      checkpoint(s) coberto(s)", exit=0. Adicionalmente conferi manualmente
      (`git diff 4614c5a..995625c -- maestro/commands/program-plan.md` e
      `grep -n '\[sweep' maestro/commands/program-plan.md`) que os 4 blocos
      `**[sweep:id]**` no arquivo correspondem 1:1 aos 4 pontos de decisão
      novos introduzidos pelo diff deste card (varrer fonte inteira; demanda
      indeterminável vai sozinha para a própria onda; teto de slices
      sugerido, não fixo; grava cartorios: via cepa-hotspots) — não sobra
      nenhum bloco de decisão do diff sem tag, e não existe tag sem bloco
      correspondente no diff (o parágrafo de descrição em prosa livre, sem
      decisão nova, não é tageado, o que é o comportamento esperado — análogo
      a uma ferramenta de cobertura de código não instrumentar comentários).
      Isto é o equivalente formal de cobertura de linha para uma prosa
      conversacional: cada unidade load-bearing do diff tem pelo menos um
      checkpoint nomeado E pelo menos uma checagem correspondente no teste de
      regressão, medido por uma ferramenta cujo próprio comportamento foi
      perturbado e confirmado load-bearing. A correção SEMÂNTICA de cada
      checagem (se ela de fato distingue prosa correta de prosa quebrada) não
      é papel do promptcov — isso já foi provado separadamente no L3 acima
      (EIXO 1 e EIXO 2, perturbação real da prosa e do artefato, GREEN->RED->
      GREEN). Os dois mecanismos juntos (promptcov mede "todo checkpoint tem
      checagem"; a perturbação do L3 mede "a checagem de fato distingue
      certo de errado") fecham a mesma lacuna dupla que uma ferramenta de
      cobertura de código convencional (JaCoCo) mede + PIT prova — não há
      mais `assumed` neste nível.
  l4_adversarial_input:
    status: n/a
    reason: >
      A mudança não abre superfície de entrada externa nova: program-plan.md
      é um comando conversacional (não parseia payload de rede/HTTP), e o
      plan.yaml deste card é um artefato de sessão, não uma API pública.
      cepa-dor (a superfície de entrada real, que já parseia YAML arbitrário)
      não foi alterado por este diff (mudou em P3, já provado em
      docs/proof/P3-cepa-dor-cartorio.yaml) — nenhum arquivo tocado por este
      card pertence a um módulo input-bearing além do próprio conteúdo
      estático de prosa/dados que o L3 já perturbou e leu de volta. O
      segundo artefato real do EIXO 3 (plan.yaml de lotes-sweep-verify) foi
      lido pelo mesmo cepa-dor já coberto por essa nota — não abre módulo
      novo. cepa-promptcov, a ferramenta que fechou l2_coverage, também não
      abre superfície nova: é CLI local que só lê 2 arquivos do próprio disco
      do usuário (ver docs/proof/promptcov-tool.yaml, mesmo n/a lá).
    findings: []
  bugfix_regression_red_at_base:
    status: n/a
    run: "issue_type=Feature (item de backlog, não Bug) — nível não aplicável."
routing_reason: >
  Os dois eixos originais aplicáveis a este card conversacional passaram com
  evidência verificada e reproduzida por mim, green→red→green, em dois
  worktrees git descartáveis distintos (/tmp/proof-P1-sweep-mode-axis1 e
  -axis2, criados a partir de 995625c, ambos removidos ao final; git status
  --porcelain limpo na árvore principal nos dois casos). Eixo 1 prova que a
  trava de regressão da prosa (tests/test_program_plan_sweep.py) é sensível a
  mudanças reais no texto que o próximo agente vai ler. Eixo 2 prova que o
  gate mecânico (cepa-dor) de fato lê e avalia o conteúdo do plan.yaml real
  produzido pela sessão de varrimento. Eixo 3 (réplica cega e independente por
  um segundo agente) reforça a confiança comportamental sem substituir medição
  de cobertura. O ÚNICO bloqueio do veredito anterior era l2_coverage: assumed
  — ausência de ferramenta que medisse quanto da prosa foi de fato exercitada
  pelo teste de regressão. Essa lacuna de instrumentação foi construída e
  provada nesta mesma sessão (common/bin/cepa-promptcov,
  docs/proof/promptcov-tool.yaml, PROVEN por outro proof-reviewer via
  perturbação das 3 decisões centrais do script). Eu mesmo reproduzi o run
  real contra o par exato deste card
  (maestro/commands/program-plan.md × tests/test_program_plan_sweep.py) ->
  4/4 checkpoint(s) coberto(s), exit=0 — e confirmei manualmente, comparando
  o diff do card aos blocos `[sweep:id]` marcados no arquivo, que os 4
  checkpoints tageados correspondem 1:1 aos 4 pontos de decisão novos que o
  diff introduziu (nenhum decisão nova ficou sem tag, nenhuma tag sobra sem
  correspondência no diff). Com isso, l2_coverage passa de assumed para pass:
  a ferramenta prova estruturalmente que todo checkpoint tem checagem
  (equivalente a cobertura de linha), e a perturbação do L3 (EIXO 1 e EIXO 2)
  já havia provado que essas checagens são semanticamente load-bearing
  (equivalente a PIT/mutação). l4 permanece n/a (nenhuma superfície de
  entrada nova, incluindo a da própria ferramenta de cobertura). bugfix
  permanece n/a (Feature, não Bug). Nenhum nível aplicável ficou
  assumed/skipped/gap/survived — PROVEN.
